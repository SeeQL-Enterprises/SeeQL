var errorMessage = document.querySelector('.errors')

if (errorMessage.innerHTML === "") {
    errorMessage.innerHTML = "Incorrect e-mail or password \<br>Click help if you forgot your credentials"
}

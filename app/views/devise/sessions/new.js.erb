var loginErrorMessage = document.querySelector('.loginErrors')

if (loginErrorMessage.innerHTML === "") {
    loginErrorMessage.innerHTML = "Incorrect e-mail or password \<br>Click help if you forgot your credentials"
}
